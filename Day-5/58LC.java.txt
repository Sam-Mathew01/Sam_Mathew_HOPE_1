class Solution {
    public int lengthOfLastWord(String s) {
        int length=0;
        for(int ind=s.length()-1;ind>=0;ind--){
            if(s.charAt(ind)!=' '){
                length++;
            }
            else{
                if(length>0) return length;
            }
        }
        return length;
    }
}